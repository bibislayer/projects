{% extends 'VMGeneralBundle:Back:layout.html.twig' %}
{% block stylesheets %}
{{ parent() }}
{% endblock %}
{% block content %}
    <div class="grid_12">
        <div class="border-main">
            <div class="block-simple">
                <div class="block_design">
                    <div class="block_header"><h1>Questionnaire {{questionnaire.name}}</h1></div>
                    <div class="block_content blocpres">
                        <div class="right show">
                            {% if questionnaire.Enterprise.logo is defined%}
                                <img src="{{ asset(questionnaire.Enterprise.logo) }}" alt="logo_entreprise" />
                            {% endif %}
                        </div>
                           {% if questionnaire.Enterprise.name is defined %}
                            <p class="eighteenpx"> <span class="bold">Entreprise : </span><span >{{questionnaire.Enterprise.name}}</span></p>
                            {% endif %}
                            {% if questionnaire.Enterprise.urlSite is defined %}
                                <p> <span class="bold">Site web : </span><span ><a href="{{questionnaire.Enterprise.urlSite}}" target="_blank">{{questionnaire.Enterprise.urlSite}}</a></span></p>
                            {% endif %}
                            <div class="textTMCE">
                               {{questionnaire.textIntroduction | raw}}
                            </div>
                            <div class="textTMCE">
                                {{questionnaire.textPresentation | raw}}
                            </div>
                            <a  class="btn_principal text_center" style="margin-left: 40% !important;" href="{{ path('bo_questionnaire_process', {'slug_quest': app.request.get('slug_quest')}) }}">{% trans with {'%name%': questionnaire.name} from 'front' %}questionnaire.actionButton.start{% endtrans %}</a>
                        <div class="stop"></div>
                    </div>
                </div>
            </div>
        </div>
    </div>
<div class="stop"></div>
{% endblock %}